#include "pch.h"
/*#include <iostream>
#include <vector>
#include <fstream>
#include "Network.h"

using t_savedata = std::vector<std::vector<std::vector<float>>>;

class DataManager {
public:
	bool serialize(const std::string& filename, t_savedata& weights) {

	}

	bool deserialize(const std::string& filename, Network& network) {
		//if (Dimensions initialized by the network and the file don't match) {
			//...
		//}
	}
};*/